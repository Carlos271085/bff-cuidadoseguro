services:
  bff:
    <<: *java-common
    build:
    context: ./bff-cuidadoseguro
    dockerfile: Dockerfile
    container_name: bff
    image: ${DOCKERHUB_USERNAME:-local}/bff-cuidadoseguro:${TAG:-latest}
    environment:
      SPRING_PROFILES_ACTIVE: docker
      CORS_ALLOWED_ORIGINS: "http://localhost:5173,http://alb-auth-cs-118174176.us-east-1.elb.amazonaws.com:8081/"
    ports:
      - "8090:8090"