server.port=8090

# Comunicacion entre contenedores por nombre de servicio (nunca localhost).
auth.url=${AUTH_SERVICE_URL:http://auth-service:8081}
pacientes.url=${PACIENTES_SERVICE_URL:http://pacientes:8082}
datosmedicos.url=${DATOS_MEDICOS_SERVICE_URL:http://datosmedicos-service:8083}

cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:5173}
